node_modules/

# Bóveda de Obsidian: cerebro del negocio (números, clientes, teléfonos).
# NO se sube — el repo se publica en Vercel. El respaldo lo da OneDrive.
Boveda/

# El manual compilado del asistente: sale de la bóveda, así que lleva lo mismo
# adentro (precios, estrategia de venta). Se pega en Ajustes y vive en Firestore.
tools/manual.txt

# Credenciales del panel para subir el manual solo. NUNCA se sube.
tools/credenciales.json

# La bitácora de pruebas: sale de la bóveda y lleva los números adentro
# (costo por venta, gasto en pauta). Vive en Firestore; esto es solo la fuente.
tools/hallazgos.json
